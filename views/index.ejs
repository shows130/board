<!DOCTYPE html>
<html>
    <head>
        <script>
            function add(){
                location.href = "/board/add";
            }
            function info(no){
                location.href = "/board/info?no="+no;
            }
            function logout(){
                location.href = "/logout"
            }
        </script>
        <%- include("./head.ejs") %>
    </head>

    <body>
        <%- include("./nav.ejs") %>
        <div class ="container" style="margin-top: 20px;">
        <div style="margin-top: 20px;">
            <div align="right">
                <button onclick="logout()" class="btn btn-danger">로그아웃</button>
            </div>
            <h2>
                게시판
            </h2>
        <table class = "table">
            <tr>
                <th>No</th>
                <th>Title</th>
                <th>Date</th>
                <th>Time</th>
                <th>img</th>
            </tr>
            <%if(content.length == 0){%>
                <tr>
                    <td colspan="5">해당 게시판은 게시글이 없습니다</td>
                </tr>
            <%}else{
                for(var i = 0; i < content.length; i++){%>
                    <tr onclick="info('<%=content[i].No%>')">
                        <td><%=content[i].No%></td>
                        <td><%=content[i].title%></td>
                        <td><%=content[i].date.slice(0,4)%>.<%=content[i].date.slice(4,6)%>.<%=content[i].date.slice(6,8)%></td>
                        <td><%=content[i].time.slice(0,2)%>:<%=content[i].time.slice(2,4)%>:<%=content[i].time.slice(4,6)%></td>
                        <td><img src="<%=content[i].img%>" width =125 height=100></td>
                    </tr>
                <%}}%>
        </table>
        </div>
        <div align="right">
            <button onclick="add()" class="btn btn-primary">글쓰기</button>
        </div>
        <div>
            
        </div>
    </body>
</html>

<!-- style="display : table-cell; vertical-align: middle;height: 100px;" -->
